[project]
name = "legal-rag"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "uvicorn",
  "pydantic>=2",
  "httpx",
  "lxml",
  "python-dateutil",
]

[tool.pytest.ini_options]
pythonpath = ["backend"]
